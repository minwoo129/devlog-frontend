import { createTheme } from "@mui/material/styles";

export const navigationTheme = createTheme({
  palette: {
    primary: {
      main: "#212121",
    },
    secondary: {
      main: "#424242",
    },
  },
});
